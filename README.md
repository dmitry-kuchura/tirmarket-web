##Пересобирать фронтенд из исходников, через npm run build (внутри лежит blendid и webpack).
##Собранные файлы копируются автоматически из _HTML/dist в Media.
##Папку Media/js/programmer НЕ перезаписывать!